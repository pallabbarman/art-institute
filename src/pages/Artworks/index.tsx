import Spinner from 'components/Spinner';

const Artworks = () => {
    return (
        <div>
            <h1>Hello world!</h1>
            <Spinner />
        </div>
    );
};

export default Artworks;
